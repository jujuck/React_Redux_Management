import { createAction } from "@reduxjs/toolkit";

export const quantity = createAction("shops/quantity");
export const status = createAction("shops/status");
export const addProduct = createAction("shop/add");
